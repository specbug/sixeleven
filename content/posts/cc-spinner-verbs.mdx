---
title: "My Claude Code Setup"
date: "February 03, 2026"
excerpt: "Terminal, worktrees, modern CLI tools, sound notifications, spinner verbs—how I've customized Claude Code."
readingTime: "6"
tags: ["ai", "diy"]
---

I use Claude Code for 100% of my dev workflows now. I haven't manually written a single line of code in ages—at this point, writing code by hand feels like fighting an eldritch horror from Elden Ring, except less satisfying. Claude Code just... works, and it's endlessly customizable. Here's how I've set mine up.

{{claude-disclaimer}}

## Terminal: Ghostty + Zellij

I run Claude Code inside [Ghostty](https://ghostty.org/) with [Zellij](https://zellij.dev/) as a multiplexer. This combo gives me:

- **Performance** - Ghostty is GPU-accelerated and noticeably snappier than iTerm2 or Terminal.app, especially when Claude is streaming long outputs
- **Parallel Claude sessions** - I run multiple Claude Code instances side by side, each working on different tasks or repos
- **Session persistence** - Zellij serializes sessions to disk, so I can detach and reattach without losing context. Useful when switching between projects

My Ghostty config (`~/.config/ghostty/config`):

```text
font-family = "R Plex Mono"
font-size = 19.0
font-thicken = true
theme = ayu
macos-option-as-alt = true
copy-on-select = true
scrollback-limit = 50000
cursor-style = bar
cursor-style-blink = false
```

The key bits: `macos-option-as-alt` lets Alt-based shortcuts work properly (Zellij uses these heavily), and a large scrollback buffer helps when reviewing Claude's longer outputs.

Zellij config (`~/.config/zellij/config.kdl`) is mostly default with vim-style navigation and tmux compatibility mode (`Ctrl+b`). The important additions:

```kdl
session_serialization true
serialize_pane_viewport true
scrollback_lines_to_serialize 5000
```

This saves session state including pane contents, so I can close my laptop and pick up exactly where I left off.

## Worktrees with ccwt

When I need to work on multiple branches simultaneously—say, a feature branch and a hotfix—I use a small script called `ccwt` that launches Claude Code in a temporary git worktree:

```bash
ccwt feature/new-auth
```

This creates a worktree for the branch in a temp directory and opens Claude Code there. Combined with Zellij panes, I can have separate Claude sessions working on completely different branches of the same repo without any git stashing or context switching. When the worktree session ends, cleanup is automatic.

## Modern CLI Tools

My shell is tuned for speed and aesthetics. These tools make the terminal feel modern:

- **[eza](https://github.com/eza-community/eza)** - `ls` replacement with icons and git status. Aliased to `ls`, `ll`, `lt`
- **[bat](https://github.com/sharkdp/bat)** - `cat` with syntax highlighting. Aliased to `cat`
- **[fd](https://github.com/sharkdp/fd)** - Fast, user-friendly `find` alternative
- **[zoxide](https://github.com/ajeetdsouza/zoxide)** - Smarter `cd` that learns your habits. Just `z proj` to jump to `~/projects`
- **[fzf](https://github.com/junegunn/fzf)** - Fuzzy finder for everything. `Ctrl+T` for files, `Alt+C` for directories, `Ctrl+R` for history
- **[atuin](https://github.com/atuinsh/atuin)** - Shell history that syncs across machines and searches intelligently
- **[yazi](https://github.com/sxyazi/yazi)** - Blazing fast TUI file manager
- **[starship](https://starship.rs/)** - Minimal, fast prompt with git info
- **[gum](https://github.com/charmbracelet/gum)** - Pretty CLI prompts for scripts

Claude Code can use these too—when I tell it to explore a directory or search for files, it often reaches for `fd` or `rg` instead of the slower built-ins.

## Sound Notifications

This one's simple but transformative. When Claude finishes a task or needs my attention, I want to know—without staring at the terminal.

```json
"hooks": {
  "Stop": [
    {
      "matcher": "",
      "hooks": [
        {
          "type": "command",
          "command": "afplay /System/Library/Sounds/Hero.aiff"
        }
      ]
    }
  ],
  "Notification": [
    {
      "matcher": "",
      "hooks": [
        {
          "type": "command",
          "command": "afplay /System/Library/Sounds/Submarine.aiff"
        }
      ]
    }
  ]
}
```

`Stop` fires when Claude completes work—Hero.aiff is a satisfying triumphant chime. `Notification` fires when Claude pauses for input or wants your attention—Submarine is a gentle "hey, look over here." macOS has a bunch of built-in sounds in `/System/Library/Sounds/` to choose from.

## Status Line

The default status line is fine, but I wanted something closer to my shell prompt. This shows user@host, the current directory, git branch (if any), and active Python virtualenv:

```json
"statusLine": {
  "type": "command",
  "command": "d=$(basename \"$(pwd)\"); g=\"\"; if git rev-parse --git-dir >/dev/null 2>&1; then b=$(git symbolic-ref --short HEAD 2>/dev/null || git rev-parse --short HEAD 2>/dev/null); if [ -n \"$b\" ]; then g=$(printf \" \\033[36m(%s)\\033[0m\" \"$b\"); fi; fi; v=\"\"; if [ -n \"$VIRTUAL_ENV\" ]; then v=$(printf \" \\033[32m(%s)\\033[0m\" \"$(basename \"$VIRTUAL_ENV\")\"); elif [ -n \"$CONDA_DEFAULT_ENV\" ]; then v=$(printf \" \\033[32m(%s)\\033[0m\" \"$CONDA_DEFAULT_ENV\"); fi; printf \"\\033[2m%s@%s\\033[0m \\033[34m%s\\033[0m%s%s\" \"$(whoami)\" \"$(hostname -s)\" \"$d\" \"$g\" \"$v\""
}
```

It's a bit of a one-liner, but it gives you something like `rishitv@mac my-project (main) (venv)` at a glance.

## Spinner Verbs

Claude Code lets you replace the default loading messages with custom ones. It's called [`spinnerVerbs`](https://www.threads.com/@boris_cherny/post/DUB4mtmEskQ), purely cosmetic, zero utility, I love it.

You're heads-down debugging something, grinding through it, and then "Quirrelling" flashes by. Or "Simply walking in". Or "Just as planned" appears right before Claude suggests exactly what you were hoping for. It snaps you out of the trance for a second, brings a smile, and you carry on.

![Touching grace...](/images/cc-spinner.png)

Here's my current list:

```text
SCIENCE!-ing
Updating priors
Applying Bayes
Noticing confusion
Making beliefs pay rent
Steelmanning
Noticing skulls
Reversing stupidity
Calibrating
Approximating CEV
Consulting dark side
Level 3 plotting
Going one higher
Time-turning
Quirrelling
Outside viewing
Trying finger, but hole
Touching grace
Git gud
Vigor checking
Hidden path ahead
Liar ahead
Fort, night
Summoning mimic
Reading item lore
Try jumping
Message appraising
Rune farming
Parrying it
Quoting papa
For those who come after
Taking hobbits to Isengard
Simply walking in
Petting the precious
Tossing dwarves
Boiling 'em, mashing 'em
Scarf wrapping
Rumbling
Reaching the basement
Tatakae-ing
Morse coding gravity
Transcending spacetime
Setting humor to 75%
Come on, TARS
Ding ding ding-ing
Adjusting tie
Bitch-ing
Cooking, Jesse
Knocking
Eating apples
Just as planned
Taking a potato chip
Questioning authority
Advertising courier services
Achieving perfect run
Quicksaving
Aranea networking
Noting for next loop
Refining macro data
Praising Kier
Meaning emerging
Self-referencing
Strange looping
Amaze!-ing
Running Crysis
Maximum armor
Bonfire lit
Praising the sun
Mikiri countering
Henry's come to see us
Reading in progress
Jesus Christ be praised
Feeling quite hungry
Petting Cerberus
Dragging
Rushing
Not quite my tempo
Soul editing
Leveling up
Min-maxing
Shard communicating
"I can explain"
Being reasonable
Double jumping
Putting ambitions to rest
Grafting
Defeating Malenia
Solo'ing her
Behold, dog!
Remembrall glowing
Drinking Comed-Tea
Breaking into Azkaban
Solving death
```

These go in `~/.claude/settings.json` under `spinnerVerbs` with `mode: replace`. Steal them or make your own.

## Plugins

I've enabled a few community plugins:

- **commit-commands** - shortcuts for git workflows
- **pr-review-toolkit** - code review helpers
- **agent-sdk-dev** - for building Claude Agent SDK apps
- **ralph-loop** - autonomous coding loops

```json
"enabledPlugins": {
  "pr-review-toolkit@claude-code-plugins": true,
  "agent-sdk-dev@claude-code-plugins": true,
  "commit-commands@claude-code-plugins": true,
  "ralph-loop@claude-plugins-official": true
}
```

## Other Settings

A few more things I keep enabled:

```json
"model": "opus",
"alwaysThinkingEnabled": true
```

Opus is slower but noticeably better for complex reasoning tasks. Extended thinking (`alwaysThinkingEnabled`) lets Claude work through problems step by step before responding—makes a real difference for debugging and architecture decisions.

---

All of this lives in `~/.claude/settings.json`. The [Claude Code docs](https://docs.anthropic.com/en/docs/claude-code) cover the full schema, but honestly the best way to figure out what works for you is just to experiment.
